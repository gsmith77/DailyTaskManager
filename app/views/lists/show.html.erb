<%= @list.title %><br><br>

<% @list.tasks.each do |task| %>
    <%= link_to task.content, task_path(task) %>
    Your Task is:<%= render partial: 'tasks/status', locals: {task: task} %>
    Have You completed this task yet? <%= button_to "Completed", url: list_update_task_path(@list) %><br><br>
<% end %><br>

<%= link_to "Create New Task", new_list_task_path(@list) %>
<%= render partial: 'error_message', locals: {task: @task} %>
<br>
<%= link_to "Back to #{current_user.name.capitalize}'s Page", user_path(current_user) %><br><br>
<%= link_to "Back to #{current_user.name.capitalize}'s List's Page", user_lists_path(current_user) %>